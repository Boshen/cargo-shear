[package]
name = "misplaced_ignored"
version = "0.1.0"
edition = "2024"

[dependencies]
# Only detected in the dev target below, so the ignore suppresses
# `misplaced_dependency` and is therefore not redundant.
anyhow = "1.0"

[package.metadata.cargo-shear]
ignored = ["anyhow"]
