[workspace]
resolver = "3"
members = ["app", "lib"]

[workspace.dependencies]
# Used in `app`, declared-but-unused dev-dependency in `lib`
anyhow = "1.0"

[workspace.metadata.cargo-shear]
ignored = ["anyhow"]
