[package]
name = "serde_attribute"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
# Never imported; the `serde(with = "...")` attribute is its only usage.
humantime-serde = "1.1"
