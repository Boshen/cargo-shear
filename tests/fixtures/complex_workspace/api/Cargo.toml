[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { workspace = true }
anyhow = { workspace = true }
core = { path = "../core" }

[build-dependencies]
# This build dep is not used
cc = "1.0"
